<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenNEL_WinUI.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenNEL_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <local:AnnouncementContent Grid.Column="0" Margin="0,0,8,0"/>
            <local:AdContent Grid.Column="1" Margin="8,0,0,0"/>
        </Grid>

        <Border x:Name="UserInfoCard" Grid.Row="1" Margin="0,8,0,0" Padding="20" CornerRadius="8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="AvatarButton" Grid.Column="0" Padding="0" BorderThickness="0" Background="Transparent" Margin="0,0,16,0"
                        Click="AvatarButton_Click" ToolTipService.ToolTip="点击更换头像">
                    <Grid Width="56" Height="56">
                        <Border x:Name="AvatarBorder" CornerRadius="12">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#6366F1" Offset="0"/>
                                    <GradientStop Color="#A855F7" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <FontIcon x:Name="AvatarIcon" Glyph="&#xE77B;" FontSize="28" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <Border CornerRadius="12" x:Name="AvatarImageBorder" Visibility="Collapsed">
                            <Image x:Name="AvatarImage" Stretch="UniformToFill"/>
                        </Border>
                    </Grid>
                </Button>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="UsernameText" Text="加载中..." FontSize="18" FontWeight="SemiBold"/>
                        <Border x:Name="RankBorder" Padding="8,2" CornerRadius="10" Background="{ThemeResource ControlFillColorSecondaryBrush}" Visibility="Collapsed">
                            <TextBlock x:Name="RankText" FontSize="12"/>
                        </Border>
                    </StackPanel>
                    <TextBlock x:Name="UserIdText" Text="" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="24,0" VerticalAlignment="Center">
                    <TextBlock Text="注册时间" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="CreatedAtText" Text="-" FontSize="13" FontWeight="Medium" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="24,0" VerticalAlignment="Center">
                    <TextBlock Text="最后登录" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="LastLoginText" Text="-" FontSize="13" FontWeight="Medium" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="4" Margin="24,0" VerticalAlignment="Center">
                    <TextBlock Text="账户状态" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="StatusText" Text="正常" FontSize="13" FontWeight="Medium" Foreground="#22C55E" Margin="0,2,0,0"/>
                </StackPanel>

                <Button x:Name="WebKeyButton" Grid.Column="5" Content="生成网页密钥" Click="WebKeyButton_Click" Margin="24,0,0,0" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Page>

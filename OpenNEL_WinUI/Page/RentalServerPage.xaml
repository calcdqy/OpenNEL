<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenNEL_WinUI.RentalServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenNEL_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:BoolNegationConverter x:Key="BoolNegationConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="租赁服" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="刷新" Click="RefreshButton_Click"/>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="未登录"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding NotLogin, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <GridView x:Name="ServersGrid" Grid.Row="1"
                  ItemsSource="{x:Bind Servers}"
                  Visibility="{Binding NotLogin, Converter={StaticResource BoolNegationConverter}}"
                  SizeChanged="ServersGrid_SizeChanged">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="6"/>
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:RentalServerItem">
                    <Grid Padding="12" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" MinWidth="240">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="{x:Bind Name}" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Row="1" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                            <Run Text="在线人数: "/><Run Text="{x:Bind PlayerCount}"/>
                        </TextBlock>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="4" Margin="0,4,0,0">
                            <FontIcon Glyph="&#xE72E;" FontSize="12" Visibility="{x:Bind HasPassword, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"/>
                            <TextBlock Text="需要密码" FontSize="12" Visibility="{x:Bind HasPassword, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"/>
                        </StackPanel>
                        <Button Grid.Row="3" Content="加入服务器" HorizontalAlignment="Left" Click="JoinServerButton_Click" Tag="{x:Bind}" Margin="0,8,0,0"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <Grid Grid.Row="2" Visibility="{Binding NotLogin, Converter={StaticResource BoolNegationConverter}}" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button x:Name="PrevPageButton" Content="上一页" Click="PrevPageButton_Click"/>
                <Button x:Name="NextPageButton" Content="下一页" Click="NextPageButton_Click"/>
            </StackPanel>
            <TextBlock x:Name="PageInfoText" Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="OpenNEL_WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenNEL_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Open NEL">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" x:Name="AppTitleBar" VerticalAlignment="Top" Background="Transparent" Margin="0,-1,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,6,0,6">
                <Image x:Name="AppTitleIcon" Width="24" Height="24" Source="ms-appx:///Assets/OpenNEL.png" Margin="0,0,8,0"/>
                <TextBlock x:Name="AppTitleTextBlock" Text="Open NEL" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <NavigationView Grid.Row="1" x:Name="NavView"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"
                        SelectionChanged="NavView_SelectionChanged"
                        Loaded="NavView_Loaded"
                        SizeChanged="NavView_SizeChanged">
            <Frame x:Name="ContentFrame"/>
        </NavigationView>

        <Grid Grid.Row="1" IsHitTestVisible="False">
            <local:NotificationHost HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,16,0,0"/>
        </Grid>

        <Grid x:Name="LoginOverlay" Grid.RowSpan="2" Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}" Visibility="Collapsed">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,24" HorizontalAlignment="Center">
                    <TextBlock Text="Open NEL" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <NavigationView x:Name="AuthNavView"
                                    PaneDisplayMode="Top"
                                    IsBackButtonVisible="Collapsed"
                                    IsSettingsVisible="False"
                                    IsPaneToggleButtonVisible="False"
                                    SelectionChanged="AuthNavView_SelectionChanged">
                        <NavigationView.MenuItems>
                            <NavigationViewItem Content="登录" Tag="Login" IsSelected="True"/>
                            <NavigationViewItem Content="注册" Tag="Register"/>
                        </NavigationView.MenuItems>
                        <NavigationView.Resources>
                            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"/>
                        </NavigationView.Resources>
                    </NavigationView>
                </StackPanel>

                <StackPanel x:Name="LoginPanel" Grid.Row="1" Spacing="16" Opacity="1">
                    <StackPanel.Transitions>
                        <TransitionCollection>
                            <EntranceThemeTransition FromHorizontalOffset="-50" FromVerticalOffset="0"/>
                        </TransitionCollection>
                    </StackPanel.Transitions>
                    <TextBox x:Name="LoginUsername" Header="用户名" PlaceholderText="请输入用户名"/>
                    <PasswordBox x:Name="LoginPassword" Header="密码" PlaceholderText="请输入密码"/>
                    <TextBlock x:Name="LoginError" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="Collapsed" TextWrapping="Wrap"/>
                    <Button x:Name="LoginButton" Content="登录" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}" Click="LoginButton_Click"/>
                    <TextBlock Text="没有账号？请切换到注册" HorizontalAlignment="Center" Opacity="0.5" FontSize="12"/>
                </StackPanel>

                <StackPanel x:Name="RegisterPanel" Grid.Row="1" Spacing="16" Visibility="Collapsed" Opacity="0">
                    <StackPanel.Transitions>
                        <TransitionCollection>
                            <EntranceThemeTransition FromHorizontalOffset="50" FromVerticalOffset="0"/>
                        </TransitionCollection>
                    </StackPanel.Transitions>
                    <TextBox x:Name="RegisterUsername" Header="用户名" PlaceholderText="请输入用户名"/>
                    <PasswordBox x:Name="RegisterPassword" Header="密码" PlaceholderText="请输入密码"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="RegisterCaptchaText" Header="验证码" PlaceholderText="请输入验证码"/>
                        <Button Grid.Column="1" x:Name="RefreshCaptchaButton" VerticalAlignment="Bottom" Margin="8,0,0,0" Click="RefreshCaptcha_Click">
                            <Image x:Name="CaptchaImage" Width="120" Height="40" Stretch="Uniform"/>
                        </Button>
                    </Grid>
                    <TextBlock x:Name="RegisterError" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="Collapsed" TextWrapping="Wrap"/>
                    <Button x:Name="RegisterButton" Content="注册" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}" Click="RegisterButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
